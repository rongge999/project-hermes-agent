# 项目智能管家（Hermes Agent）需求文档

> 文档版本 V1.0 | 2026-05-07 | Hermes 需求组

## 1. 项目背景

当前个人开发者同时维护多个项目（编号 #001～#009 及后续新增），涉及 AI 修图引擎、小程序垂直行业模板、量化交易系统、音频转笔记等不同领域。日常工作中存在以下痛点：

- **项目管理碎片化**：任务、文档、会议纪要分散在飞书、本地备忘录、代码仓库评论区，缺乏统一视图和自动化跟踪。
- **原子能力难以沉淀**：可复用的代码片段、算法、预设值（如自动扶正、人像参数、行业模板）散落在各项目中，新项目启动时重复造轮子。
- **知识孤岛**：飞书知识库中的文档与代码实际逻辑脱节，更新不及时。
- **缺乏复用度量**：无法客观评估哪些能力真正有价值，哪些需要优化或弃用。

为解决上述问题，拟开发「项目智能管家」（代号 Hermes Agent），利用大语言模型和自动化流程，深度集成飞书、Git 仓库，实现项目全生命周期的智能辅助与原子能力闭环管理。

## 2. 项目目标

### 2.1 总体目标

构建一个以 AI Agent 为核心的轻量级项目管理与能力沉淀平台，将用户的开发效率提升 30% 以上，同时将零散的经验固化为可持续复用的数字资产。

### 2.2 具体目标

1. **自动化项目跟踪**：从对话、代码提交、会议讨论中自动提取任务，更新看板，生成周报/纪要。
2. **原子能力主动识别**：扫描代码变更和文档，发现可封装的通用逻辑，自动生成能力草稿。
3. **智能复用推荐**：在新功能开发或代码审查时，主动推荐已有能力，避免重复实现。
4. **知识库一致性维护**：监控文档变化与代码实际行为，标记不一致或过时内容。
5. **度量与优化闭环**：统计能力复用次数、节省工时，输出月度报告，指导能力维护。

## 3. 用户角色

- **主要用户**：个人全栈开发者（拥有所有权限）。
- **扩展用户（远期）**：小规模团队（3-5人），角色分为管理员、开发者、访客。

## 4. 功能需求

### 4.1 项目管理模块

| 编号 | 功能 | 描述 | 优先级 |
|:---|:----|:-----|:------|
| PM-01 | 飞书多维表格同步 | 自动将识别到的任务写入《项目看板》多维表格，支持任务状态、优先级、关联项目编号 | P0 |
| PM-02 | 会议纪要生成 | 监听飞书会议录音或文字记录，生成结构化纪要，提交到对应项目的文档夹 | P1 |
| PM-03 | 周报/日报生成 | 每周五或触发指令，汇总本周完成事项、下周计划、风险变更 | P1 |
| PM-04 | 里程碑检测 | 识别项目看板中任务完成率达到 100% 或用户声明"结项"，自动归档文档 | P2 |

### 4.2 原子能力管理模块

| 编号 | 功能 | 描述 | 优先级 |
|:---|:----|:-----|:------|
| CAP-01 | 能力清单管理 | 维护《原子能力清单》多维表格，含能力ID、名称、描述、输入/输出、调用方式、来源项目、复用次数 | P0 |
| CAP-02 | 主动识别与草稿生成 | 监听 Git commit 消息和 PR 文件变更，发现通用逻辑跨项目复用≥2次时自动创建能力草稿 | P0 |
| CAP-03 | 能力检索与推荐 | 支持自然语言搜索能力，在新项目启动或代码审查时主动推送 | P0 |
| CAP-04 | 能力版本管理 | 能力代码更新时记录版本号，通知依赖项目 | P2 |
| CAP-05 | 调用统计与评价 | 用户可在飞书点击"有用/无用"，记录推荐采纳率 | P1 |

### 4.3 知识库维护模块

| 编号 | 功能 | 描述 | 优先级 |
|:---|:----|:-----|:------|
| KB-01 | 文档-代码关联 | 允许在飞书文档中嵌入代码链接，Agent 定期检查有效性 | P1 |
| KB-02 | 孤儿文档检测 | 扫描超过30天无访问且无链接的文档，提示归档 | P2 |
| KB-03 | 变更传播提醒 | 能力代码修改后，查找所有引用文档发送同步提醒 | P1 |

### 4.4 智能报告模块

| 编号 | 功能 | 描述 | 优先级 |
|:---|:----|:-----|:------|
| RP-01 | 能力复用月度报告 | 每月1日生成各能力调用次数、节省工时、未使用列表 | P1 |
| RP-02 | 项目健康度看板 | 动态展示任务逾期率、文档完整度、能力复用率 | P2 |

## 5. 非功能需求

### 5.1 性能要求
- 任务识别到写入多维表格延迟 < 5 秒
- 能力检索响应时间 < 2 秒

### 5.2 可靠性
- Agent 服务可用性 ≥ 99.5%
- 所有自动化操作需有操作日志，支持回滚

### 5.3 安全性
- 仅授权飞书租户内用户可调用 API
- Git 仓库只读取 commit 消息和文件路径
- 敏感信息（API密钥）需加密存储

### 5.4 可扩展性
- 能力调用方式支持 HTTP API、命令行、代码片段三种形式
- 允许用户自定义触发规则

### 5.5 技术保值性
- 核心后端使用 Python (FastAPI)，数据库 PostgreSQL + Redis
- 依赖开源大模型或本地部署模型（Ollama）

## 6. 技术栈建议

| 层次 | 技术选型 | 说明 |
|:----|:---------|:-----|
| 前端 | 飞书小程序卡片 | 初期通过飞书消息交互，远期做 Web 仪表盘 |
| 后端 | Python FastAPI | 快速开发，便于集成大模型 SDK |
| Agent 框架 | LangChain 或 OpenAI 函数调用 | 工具调用（飞书表格、Git 扫描） |
| 数据存储 | PostgreSQL + Redis | 能力清单、任务缓存、用户反馈 |
| 集成 | 飞书开放 API、Git Webhook | 读取表格、文档、评论 |
| 部署 | Docker + 轻量服务器（2C4G） | 月成本约 60 元 |

## 7. 约束与依赖

- 必须依赖飞书多维表格
- 依赖 Git 仓库 Webhook
- 大模型 API 成本预估 5~10 美元/月（可用本地 Ollama 替代）
- MVP 开发周期约 2 周（每天 2 小时）

## 8. 验收标准

1. 成功监听一次会议录音，输出含"决议""待办""风险"的纪要
2. 扫描 #001 项目 Git 提交，自动识别并生成至少 1 条原子能力草稿
3. 用户在飞书输入"需要裁图扶正"，Agent 回复已有能力调用方式
4. 能力被调用后，"有用"按钮可将复用次数 +1
5. 每月初自动生成能力复用报告

## 9. 后续迭代方向

| 版本 | 功能 |
|:----|:-----|
| V1.0 | 基础项目管理 + 原子能力识别 + 飞书集成 |
| V1.1 | 支持多用户团队、权限分离 |
| V1.2 | 接入大模型实现自然语言创建子任务 |
| V1.3 | 能力代码自动生成 |
| V2.0 | 跨平台 + 本地知识库向量检索 |
